services:
  app:
    build: .
    container_name: mcp-langgraph-agent
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    networks:
      - shared_net
    restart: unless-stopped

networks:
  shared_net:
    external: true
